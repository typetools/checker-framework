daikon.tools.runtimechecker.Main: runtime-check instrumenter
The runtime check instrumenter creates new versions of Java source files.
The new versions behave in the same way as the originals, but
invariant properties are checked at runtime.

The instrumenter can operate in the following modes, indicated by the first
argument to the daikon.tools.runtimecheker.Main class:

help
  Print a usage message

