When building the plugin, checker.jar and checker-qual.jar should be copied to checker-framework\netbeans\CheckerFrameworkPlugin\release

To build the Netbeans Checker Framework module, the makenbm task, which is included as part of the NetBeans build infrastructure, can be used.