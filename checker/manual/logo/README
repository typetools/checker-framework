The add-favicon script was copied from the plume-lib library.

It should be updated from plume-lib periodically, via:

    wget https://raw.githubusercontent.com/mernst/plume-lib/master/bin/add-favicon -O add-favicon
