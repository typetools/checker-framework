// A stub file that contains more than one package declaration, with a different annotation
// imported before each one.  Every import applies to the whole file, not just to the package
// declaration that follows it.

package java.net;

import org.checkerframework.checker.tainting.qual.Tainted;

public class IDN {
    public static @Tainted String toASCII(String input);
}

package java.util;

import org.checkerframework.checker.tainting.qual.Untainted;

public class UUID {
    public @Untainted String toString();
}
