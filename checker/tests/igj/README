The IGJ tests use a different framework than all the other tests.
We should probably standardize.
See file checker-framework/checker/tests/src/tests/IGJTest.java

To add a new file to the test suite, see
  ../README

To run the tests, do
  ant -e -find build.xml igj-tests
