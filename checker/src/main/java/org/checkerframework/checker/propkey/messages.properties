# This key is not issued anywhere.  PropertyKeyAnnotatedTypeFactory detects the problem, but
# reporting a diagnostic requires an AST node and a missing properties file has none, so the
# problem is reported as a plain warning instead.
propertykeychecker.filenotfound=Could not find the properties file %s
