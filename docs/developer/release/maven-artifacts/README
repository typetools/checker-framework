The POMs in this folder are used solely to deploy artifacts to Maven Central or
a local repository. They provide meta-data about the artifacts to the repository.

${checkerframeworkversion} is changed by the Gradle tasks to be the correct verion number.

File `README-jdk` contains information for the `jdk8-source.jar` and
`jdk8-javadoc.jar` artifacts and is used in `checker/build.gradle`.
