This directory shows how to create:
 * a specialized fenum annotation, @MyFenum

The Makefile shows how to invoke the Fenum Checker to use the specialized annotation.

You can model your own new fenum annotations and fenum checkers after this example.

To see the demo, run: make demo
