This directory contains an example Maven project that downloads the latest
Checker Framework framework-all Maven artifacts and runs the Value Checker.

To run the example, simply execute:
mvn compile

You should see output that includes:

MavenExample.java:[24,28] error: [assignment.type.incompatible] incompatible types in assignment.

Note that error-prone only needs to be used in a Java 8 environment.
For Java 9+ environments, remove mentions of error-prone from pom.xml.
