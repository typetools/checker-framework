TODO: explain how the website is created 

To build eclipse-projects.zip 
  make eclipse-projects.zip
(this copies the required jars before zipping and removes any class files
before zipping)

To build sourcefiles.zip:
  make sourcefiles.zip
(this copies the source files from the eclipse-projects, copies required jars, 
and removes any class files before zipping.) 

To build both:
  make

